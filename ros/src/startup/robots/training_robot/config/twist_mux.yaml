twist_mux:
  ros__parameters:

    topics:
      autonomy:
        topic: /autonomy/cmd_vel
        timeout: 0.5
        priority: 50

      teleop:
        topic: /teleop/cmd_vel
        timeout: 0.5
        priority: 10

    locks:
      emergency_stop:
        topic: /e_stop
        timeout: 0.0
        priority: 100
